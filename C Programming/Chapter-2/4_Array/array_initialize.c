#include<stdio.h>
int main()
{
    int ar[5]={100}; // declaring and initializing an array of size 5 with the first element as 100
    for(int i=0;i<5;i++)
    {
        printf("%d\n",ar[i]); // printing the values of the array
    }
    return 0;
}