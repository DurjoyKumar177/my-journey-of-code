#include<bits/stdc++.h>

using namespace std;
int main()
{
    int x;
    cin>>x;
    cin.ignore();//to avoid a character;
    //getchar();we can also use that;
    string s;
    getline(cin,s);
    return 0;
}